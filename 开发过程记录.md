# 留言板系统 - 开发过程记录

## 项目概述

基于 SpringBoot2 + Vue2 + JDK8 + MyBatis Plus + MySQL 的留言板系统，用于已上线主系统的问题反馈和需求建议收集。

## 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 后端框架 | Spring Boot | 2.7.18 |
| 前端框架 | Vue | 2.6.14 |
| UI组件库 | Element UI | 2.15.14 |
| ORM框架 | MyBatis Plus | 3.5.3.1 |
| 数据库 | MySQL | 8.0+ |
| JDK | Java | 1.8 |
| 构建工具 | Maven | 3.6+ |

## 项目结构

```
message-board/
├── message-board/          # 后端项目（SpringBoot）
├── message/                # 前端项目（Vue2）
├── sql/                    # 数据库SQL
└── README.md               # 启动说明
```

- 后端文件夹名：`message-board`，访问路径：`http://localhost:8088/message-board/api/...`
- 前端文件夹名：`message`，访问路径：`http://localhost:8080/message/#/?uid=xxx`

## 核心功能

### 1. 留言发布
- 支持两种分类：问题反馈（question）、需求建议（requirement）
- 发布后所有人可见
- 标题最长100字，内容最长2000字

### 2. 评论回复
- 支持多层嵌套评论（树形结构，通过 parent_id 实现）
- 支持 @ 回复特定人
- 顶级评论 parent_id = 0，子回复记录被回复人信息

### 3. 点赞功能
- 点击切换点赞/取消点赞
- 通过 `tb_like` 表的 `message_id + uid` 联合唯一索引防重复
- 实时更新点赞计数

### 4. 热榜排行
- 两个维度：点赞热榜 TOP 10、评论热榜 TOP 10
- 嵌入首页右侧栏，上下排列展示
- 前三名用 🥇🥈🥉 标识

### 5. 用户集成
- 从其他系统通过 URL 参数传入 UID
- 后端从请求头 `X-User-Uid` 自动获取
- 查询外部库 `syngbs` 获取用户信息：
  - `syngbs.manex`：通过 `id`（UID）查询 `xm`（姓名）、`jgid`（机构ID）
  - `syngbs.organ`：通过 `id`（jgid）查询 `jgmc`（机构名称）
- 用户姓名和机构冗余存储到本系统表中

### 6. 操作日志
- 通过 AOP 切面自动记录：发布留言、评论、点赞、取消点赞
- 记录操作人、操作类型、描述、关联留言ID、IP地址
- 存储到 `tb_operation_log` 表

### 7. 登录用户展示
- 页面右上角显示当前用户姓名（xm）和机构名称
- 通过 `/api/user/current` 接口获取

## 数据库设计

### 本系统数据库：message_board（4张表）

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| tb_message | 留言表 | id, title, content, category, uid, user_name, jgid, org_name, like_count, comment_count |
| tb_comment | 评论表 | id, message_id, content, uid, user_name, parent_id, reply_to_uid, reply_to_name |
| tb_like | 点赞表 | id, message_id, uid（联合唯一索引） |
| tb_operation_log | 操作日志表 | id, uid, user_name, operation_type, description, message_id, ip |

### 外部依赖库：syngbs（已存在，只读）

| 表名 | 用途 |
|------|------|
| syngbs.manex | id→xm(姓名), jgid(机构ID) |
| syngbs.organ | id→jgmc(机构名称) |

## API 接口

### 留言接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/message/list | 分页查询留言列表 |
| GET | /api/message/detail/{id} | 获取留言详情（含评论树） |
| POST | /api/message/publish | 发布留言 |
| POST | /api/message/like | 点赞/取消点赞 |
| GET | /api/message/hot/like | 点赞热榜 TOP 10 |
| GET | /api/message/hot/comment | 评论热榜 TOP 10 |

### 评论接口
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/comment/publish | 发表评论/回复 |

### 用户接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/user/current | 获取当前登录用户信息 |

## 开发中遇到的问题及解决方案

### 1. JDK8 不支持 var 关键字
- **问题**：AOP 日志切面中使用了 `var userInfo = externalUserService.getUserInfo(uid)`
- **解决**：改为明确类型声明 `UserInfoDTO userInfo = externalUserService.getUserInfo(uid)`

### 2. MySQL 8.0 公钥检索错误
- **问题**：`Public Key Retrieval is not allowed`
- **解决**：JDBC URL 添加 `&allowPublicKeyRetrieval=true`

### 3. MySQL 连接认证失败
- **问题**：`Access denied for user 'root'@'localhost'`
- **解决**：需根据实际环境修改 `application.yml` 中的用户名和密码

### 4. UID 自动获取
- **需求变更**：用户不应手动输入 UID，应从其他系统传入自动获取
- **解决**：
  - 前端：URL 参数 `?uid=xxx` → 存入 Vuex → 请求拦截器自动注入请求头 `X-User-Uid`
  - 后端：创建 `UidUtils` 工具类从请求头获取 UID，Controller 层自动获取并传给 Service
  - DTO 去掉 uid 字段，Service 方法签名增加独立 uid 参数

### 5. 首页布局调整（多次迭代）
- **问题**：初始布局偏移动端风格，在电脑上不够大气
- **解决**：
  - App.vue 主容器改为 `max-width: 1400px`，左右内边距 40px
  - 首页左右布局，左侧留言列表 + 右侧热榜（320px），间距 32px
  - 顶部增加4个统计卡片（留言总数/问题反馈/需求建议/累计点赞）
  - 分类切换使用 `el-radio-button` 替代 tabs
  - 留言卡片优化：左右布局、彩色分类标签、独立点赞/评论统计区、用户头像
  - 热榜改为渐变色标题栏，前三名用 🥇🥈🥉，sticky 固定定位

### 6. 详情页宽度问题（多次迭代）
- **问题**：详情页受 App.vue 的 `max-width: 1400px` 限制，在电脑上显示很窄
- **尝试1**：改详情页 `max-width: 100%` → 无效，仍受父容器限制
- **尝试2**：用 `margin` 负值突破 → 导致横向滚动条
- **最终方案**：在 App.vue 中通过路由监听，详情页时动态添加 `is-detail` class，覆盖为 `max-width: none; padding: 24px 0 0 0`，详情页自己控制 `padding: 0 60px` 留白
- 详情页采用语雀/飞书文档风格：大标题（32px）、正文（17px/行高2.1）、宽敞内边距（52px/64px）、sticky 操作栏

### 7. 热榜嵌入首页
- **需求变更**：热榜不单独页面，直接嵌入首页右侧
- **解决**：在 MessageList.vue 中直接引入热榜数据获取和渲染，去掉 `/hot` 路由和"热榜排行"按钮

### 8. 跨域配置
- 后端添加 `WebMvcConfig`，允许跨域访问
- 前端 `vue.config.js` 配置代理，`/message-board` 代理到 `http://localhost:8088`

### 9. 前后端路径配置
- 后端 `server.servlet.context-path: /message-board`
- 前端 `publicPath: /message`，`base: /message/`
- 前端代理 `/message-board` → 后端 `8088`
- API baseURL：`/message-board/api`

## 启动方式

```bash
# 1. 初始化数据库
mysql -u root -p < sql/message_board.sql

# 2. 修改 application.yml 数据库连接信息（用户名、密码）

# 3. 启动后端（端口8088）
cd message-board
mvn spring-boot:run

# 4. 启动前端（端口8080）
cd message
npm install
npm run serve

# 5. 访问
http://localhost:8080/message/#/?uid=你的UID
```

> 数据库账号需要有 `message_board` 和 `syngbs` 两个库的查询权限。
